name: daily-report-2
on:
  push:
    branches: [main]
  schedule:
    - cron: '3 22 * * *'
  workflow_dispatch:
jobs:
  generate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "hello" > test.txt
      - run: cat test.txt
