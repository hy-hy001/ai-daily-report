name: bisect-b
on:
  push:
    branches: [main]
  schedule:
    - cron: '3 22 * * *'
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "hello with all triggers"
