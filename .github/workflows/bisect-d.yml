name: AI 日报测试
on:
  push:
    branches: [main]
  schedule:
    - cron: '3 22 * * *'
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "chinese name test"
